apiVersion: 1
datasources:
 - name: influxdb
   type: influxdb
   url: $INFLUXDB_URL
   password: $INFLUXDB_ADMIN_USER
   user: $INFLUXDB_ADMIN_PASSWORD
   database: $INFLUXDB_DB
   access: proxy
   isDefault: true
